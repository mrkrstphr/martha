build:
  - command: curl -s https://getcomposer.org/installer | php
  - command: php composer.phar install --prefer-dist --dev
  - command: phpcs --standard=PSR2 --report=checkstyle --report-file=${outputdir}/checkstyle.xml src/
